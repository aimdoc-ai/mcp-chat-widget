// Entry point for the MCP Chat Widget web component
import './components/mcp-chat-webcomponent';

// This file serves as the entry point for bundling the web component
// It imports the web component definition which registers the custom element
// No need to export anything as the component registers itself with the browser
